################################################################
# Change the below parameters according to your desired setup. 
#
# Be sure that every line has the following format, 
# including the comma at the end of the line:
#
# 'label': value,
#
# Any line that starts with the pound sign (#) is "commented" and
# will be ignored. To "uncomment" means to remove the pound sign. 
# For many parameters, there are more than one option, but only one
# should be uncommented. 
#
# After editing the file, save it. Each time you run the program, it
# will read this parameters.txt file.
#
# See README.md for explanation of each parameter.
################################################################

{
    ##### Filename slug #####
    # use whatever text you want to add as a prefix
    # for the filename of exported result data
    'filename slug': 'test',

    ##### Image type #####
    # use 'none' to avoid saving a plot of the results
    # to make a plot and save the image, specify the file type
    # e.g., 'svg', 'png', etc.
    'image type': 'svg',

    ##### Retain profile data? Keeping it can be memory-intensive. #####
    'retain profile data': False,

    ##### Parallel processing? #####
    'multiprocessing': True,

    ##### Units #####
    # use the singular unit, e.g., 'micrometer' and 'nanosecond'
    'length unit': 'micrometer',
    'time unit': 'nanosecond',

    ##### Number of simulation iterations per unique parameter set #####
    'number of runs': 1000,

    ##### Spatial parameters #####
    'spatial width': 5,
    'pixel width': 100,

    ##### Time axis parameter #####
    # Uncomment only one of the following lines:
    'time range': [0, 1, 10], # start, stop, steps (inclusive)
    # 'time series': [1,3,5,10,15,25,60,100],

    ##### Initial profile parameters #####
    'amplitude_0': 1,
    'mean_0': 0,
    # Uncomment either FWHM or sigma:
    'FWHM_0': 1,
    # 'sigma_0': 0.4032,

    ##### Nominal diffusion length #####
    # Remember to use values that correspond to the length and/or time units
    # you specified above (or squared units in the case of diffusion).
    #
    # Uncomment only one of the following lines:
    # 'nominal diffusion coefficient': 0.01, 'nominal lifetime (tau)': 1, 
    'nominal diffusion length': 0.1,

    ##### Noise standard deviation #####
    # See the readme file for a complete explanation

    # Uncomment only one of the following lines:
    'noise value': 0.05,
    # 'estimate noise from data': 'example_profile.csv',
    # 'noise range, reciprocal log': [0.01, 0.3],   # create a distribution of noise values
    # 'noise range, reciprocal': [0.01, 0.3],       # create a distribution of noise values

    # If a noise range is selected, indicate the number of values to calculate in the range.
    # This will be ignored if either 'noise value' or 'estimate noise from data' is selected, 
    # since in those cases there is only one noise value.
    'noise range, number of values': 1,

    ##### estimate/nominal proximity to 1 #####
    # e.g., proximity level of 0.5 means:
    # diffusion estimate/nominal = 1 +/- 0.5
    'proximity level': 0.5,
}
